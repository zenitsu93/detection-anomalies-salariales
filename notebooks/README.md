# Expérimentation complémentaire

Installer `requirements-notebook.txt` puis ouvrir le notebook depuis la racine ou ce dossier. Les sorties enregistrées sont celles de la version source conservée ; elles ne constituent pas une nouvelle exécution pendant la reconstruction. Les modèles et exports se régénèrent dans `models/` et `output/classification/`.
